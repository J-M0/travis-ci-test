language: python

matrix:
  include:
    - dist: bionic
      python: 3.7
    - os: osx
      osx_image: xcode10.2
      language: shell

script: python -m unittest test.py || python3 -m unittest test.py